# benchmark-service client

HTTP client and sandbox types for talking to a deployed Vals benchmark
service: `benchmark_service.client.BenchmarkServiceClient` plus the request
and response schemas. Built from the create-benchmark-service repository for
vendoring into lab deliverables.
