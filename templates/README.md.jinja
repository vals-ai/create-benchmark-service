# {{ benchmark_name }} Service

## Getting started

Edit `src/{{ benchmark_package }}/benchmark_service.py` and implement the six abstract methods on `ExampleBenchmark`:

| Method | What to do |
|--------|------------|
| `load_datasets()` | Return `dict[dataset_name, dict[task_id, ...]]` of all datasets |
| `retrieve_task()` | Return sandbox source, problem statement, and resource requirements |
| `setup_task()` | Prepare the sandbox before evaluation (upload files, run install commands) |
| `evaluate_response()` | Score a plain-text response without a sandbox |
| `evaluate_instance()` | Run tests / evaluation scripts in the sandbox |
| `calculate_final_score()` | Aggregate per-task results into a single score |

`setup_task` and `evaluate_instance` are async generators — yield `StreamMessageChunk`, `StreamErrorChunk`, and finally `StreamResultChunk`.

## Authentication

{% if template == "vals-ai" %}
This service includes the Vals layer in `src/{{ benchmark_package }}/vals_ai/`. The generated service uses it for Descope authentication, tenant/dataset access, trial response filtering, and evaluation quotas.

Set `DESCOPE_PROJECT_ID` and configure access through `BENCHMARK_CATALOG_API_URL` plus `SERVICE_NAME`, `DESCOPE_TENANT_ALLOWLIST_JSON`, or `DESCOPE_ALLOWLIST_PATH`. Hosted requests require a Descope access key in `X-Descope-Api-Key`. Quota-enabled services also require `SERVICE_NAME` and `EVALUATION_QUOTA_TABLE_NAME`.

The Vals files belong to this service. Updating the framework dependency does not update these copied files.
{% else %}
This service contains no Vals authentication or access-policy code. Implement `check_auth()` for boolean authentication, or `resolve_tenant()` and `check_dataset_access()` for tenant-aware access. The default rejects unauthenticated requests.
{% endif %}

Set `AUTH_DISABLED=true` only for local development. `/v1/*` requires authenticated tenant identity and still rejects requests in this mode.

## Optional integrations

Add `daytona`, `modal`, `s3`, or `telemetry` extras to the `create-benchmark-service` dependency in `pyproject.toml` when needed. Keep its generated Git URL and version pin. For example, `create-benchmark-service[daytona]` enables Daytona sandboxes. `s3` enables submission storage; `telemetry` enables Sentry and OpenTelemetry.
{% if template == "vals-ai" %}

This template already selects `s3` and `telemetry`. Add a sandbox provider extra for benchmarks that use sandboxes.
{% endif %}

## Development

```bash
make install   # install dependencies
make dev       # run local server
make test      # run tests
make help      # list all commands
```

## Releasing

Versions come from git tags (hatch-vcs). To enable the release flow in this repo:

- Add a **`GH_PAT`** repository secret — a personal access token with permission to push tags (`contents: write`). `auto-tag-release` uses it to push the version tag on merge to `main`; the workflow fails without it.
- Every PR title must include **`#patch`**, **`#minor`**, or **`#major`** — `check-pr-title` enforces this and the tag is bumped accordingly on merge.

Until the first tag exists, the package builds at version `0.0.0`.
